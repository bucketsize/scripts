#!/bin/sh

icons="/usr/share/icons/HighContrast/32x32/apps/vim.png"
icon=""
for icon in $icons; do
    if [ -f $i]; then
        urxvt -icon $icon -e vim $*
        exit
    fi
done
urxvt -name tvim -e vim $*
