#!/bin/sh

# opendesktop autostart
# TODO:
# 2. download and cycle through wallpapers
# 3. brightness control
# 4. complete shortcuts
# 9. fix compositing; compton is crashing in alpine

stopAll(){
	(sleep 1 && killall picom)&
	(sleep 1 && killall dunst)&
	(sleep 1 && killall tint2)&
	(sleep 2 && killall skhkd)&
	(sleep 2 && killall lxpolkit)&
	(sleep 2 && killall lxclipboard)&
	(sleep 3 && killall xautolock -exit)&
	(sleep 3 && killall light-locker)&
	(sleep 3 && pkill -f daemon.lua)&
	(sleep 3 && killall conky)
}
startAll(){
	(sleep 1 && ~/scripts/sys_ctl/ctl.lua cmd dtop_viga)&
	(sleep 2 && ~/scripts/xdg/x.wallpaper.sh cycle)&
	(sleep 2 && picom)&
	(sleep 2 && dunst)&
	(sleep 2 && tint2)&
	(sleep 2 && sxhkd -c ~/.config/sxhkd/sharedrc ~/.config/sxhkd/winrc)&
	(sleep 3 && lxpolkit)&
	(sleep 3 && lxclipboard)&
	(sleep 3 && xautolock \
	 -time 3 -locker "~/scripts/sys_ctl/ctl.lua fun scr_lock_if" \
	 -killtime 10 -killer "notify-send -u critical -t 10000 -- 'Killing system ...'" \
	 -notify 30 -notifier "notify-send -u critical -t 10000 -- 'Locking system ETA 30s ...'")&
	(sleep 4 && light-locker --lock-on-suspend --lock-on-lid)&
	(sleep 4 && ~/scripts/sys_ctl/ctl.lua fun pa_set_default)&
	(sleep 4 && ~/scripts/sys_mon/daemon.lua)&
	(sleep 5 && conky)&
}

case $i in
    stop)
        stopAll
        ;;
    *)
        stopAll
        startAll > /var/tmp/autostart.out
        ;;
esac



